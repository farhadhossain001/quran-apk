workflows:
  android-apk:
    name: Build APK
    environment:
      flutter: stable

    scripts:
      - flutter pub get
      - flutter build apk --release --split-per-abi

    artifacts:
      - build/app/outputs/flutter-apk/*.apk
